project Test is
   for Source_Dirs use (".");
   for Object_Dir use ".";
   for Exec_dir use ".";
   for Main use ("test.adb");
   package Format is
      for Width ("Ada") use "59";
      for Width ("test_dep.ads") use "79";
   end Format;
end Test;
