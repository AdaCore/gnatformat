description: With an explicitly loaded project, test that unknown sources are
  not resolved correctly and that GNATformat exits with the correct status
  code.
driver: gnatformat
args: ["-P", "test/test.gpr", "test/test_dep.adb", "--pipe"]
status_code: 1
