description: |
  Test that the CLI --indentation overwrites the project one and that the
  project Indentation_Continuation overwrites the default one.
driver: gnatformat
args: ["test.adb", "--pipe", "--indentation", "5"]
