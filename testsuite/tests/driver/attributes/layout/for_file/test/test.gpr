project Test is
   for Source_Dirs use (".");
   for Object_Dir use ".";
   for Exec_dir use ".";
   for Main use ("test.adb");
   package Format is
      for Layout ("test.adb") use "tall";
   end Format;
end Test;

